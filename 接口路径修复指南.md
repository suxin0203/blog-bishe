# 接口路径修复指南

## 问题说明

小程序代码中使用了错误的接口路径 `/wechatlogin/`，但后端实际路径是 `/wechat/`。

根据后端代码 `Express/app.js` 中的路由挂载：
```javascript
app.use('/wechat', wechatloginRouter);
```

所以正确的接口路径应该是 `/wechat/`，而不是 `/wechatlogin/`。

---

## 需要修复的文件

### 1. wenzhan_miniprogram/utils/auth.js

**第 40 行左右**，将：
```javascript
return get(`/wechatlogin/openid/${code}`)
```

改为：
```javascript
return get(`/wechat/openid/${code}`)
```

---

### 2. wenzhan_miniprogram/api/index.js

**第 350-360 行左右**，将整个 wechat 对象：

```javascript
const wechat = {
  // 通过 code 获取 openid
  getOpenId(code) {
    return get(`/wechatlogin/openid/${code}`)
  },

  // 通过 openid 获取用户信息
  getUserInfo(openid) {
    return get(`/wechatlogin/userinfo/${openid}`)
  },

  // 绑定 openid 到用户账号
  bindOpenId(userId, openid) {
    return put(`/wechatlogin/token/bind/${userId}`, { openid }, { needAuth: true })
  }
}
```

改为：

```javascript
const wechat = {
  // 通过 code 获取 openid
  getOpenId(code) {
    return get(`/wechat/openid/${code}`)
  },

  // 通过 openid 获取用户信息
  getUserInfo(openid) {
    return get(`/wechat/userinfo/${openid}`)
  },

  // 绑定 openid 到用户账号
  bindOpenId(userId, openid) {
    return put(`/wechat/token/bind/${userId}`, { openid }, { needAuth: true })
  }
}
```

---

### 3. docs/API接口索引.md

**第 172 行左右**，将：

```markdown
| `/wechatlogin/openid/{code}` | GET | 否 | 根据 code 换取 openid |
| `/wechatlogin/userinfo/{openid}` | GET | 否 | 根据 openid 获取用户信息 |
```

改为：

```markdown
| `/wechat/openid/{code}` | GET | 否 | 根据 code 换取 openid |
| `/wechat/userinfo/{openid}` | GET | 否 | 根据 openid 获取用户信息 |
```

---

## 修复方法

### 方法 1：使用查找替换（推荐）

在 VS Code 中：
1. 按 `Ctrl+Shift+H` 打开全局查找替换
2. 查找：`/wechatlogin/`
3. 替换为：`/wechat/`
4. 在文件范围中选择 `wenzhan_miniprogram` 和 `docs` 文件夹
5. 点击"全部替换"

### 方法 2：手动修改

按照上面列出的文件，逐个打开并修改对应的行。

---

## 验证修复

修复完成后，搜索整个项目中是否还有 `/wechatlogin/` 字符串：

```bash
# 在项目根目录执行
grep -r "wechatlogin" wenzhan_miniprogram/ docs/
```

如果没有输出，说明修复完成。

---

## 后端实际路由挂载（参考）

根据 `Express/app.js`：

```javascript
app.use('/wechat', wechatloginRouter);        // ✅ 微信登录
app.use('/qr-login', qrLoginRouter);          // ✅ PC扫码登录
app.use('/miniapp', miniappQrLoginRouter);    // ✅ 小程序扫码
app.use('/points', pointsRouter);             // ✅ 积分系统
app.use('/articles', articleRouter);          // ✅ 文章
app.use('/users', usersRouter);               // ✅ 用户
app.use('/categories', categoryRouter);       // ✅ 分类
app.use('/tags', tagRouter);                  // ✅ 标签
app.use('/comments', commentRouter);          // ✅ 评论
app.use('/likes', articleLikeRouter);         // ✅ 点赞
app.use('/favorites', articleFavoriteRouter); // ✅ 收藏
app.use('/messages', messageRouter);          // ✅ 留言
app.use('/swiper', swiperRouter);             // ✅ 轮播图
app.use('/friendslink', friendslinkRouter);   // ✅ 友情链接
app.use('/otherswitch', otherswitchRouter);   // ✅ 全局配置
app.use('/dashboard', dashboardRouter);       // ✅ 数据看板
app.use('/upload', uploadJPGRouter);          // ✅ 文件上传
app.use('/activity', ActivityRouter);         // ✅ 活动签到
app.use('/utils', utilsRouter);               // ✅ 工具
```

所有接口路径都应该与这些路由挂载保持一致。

---

**修复完成后，请测试微信登录功能确保正常工作。**
